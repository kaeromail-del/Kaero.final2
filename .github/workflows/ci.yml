name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  backend:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: backend/package-lock.json
    - name: Install dependencies
      run: cd backend && npm ci
    - name: Lint
      run: cd backend && npm run lint
    - name: Type check
      run: cd backend && npm run typecheck
    - name: Test
      run: cd backend && npm test
    - name: Build
      run: cd backend && npm run build

  deploy-backend:
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: backend

    steps:
    - uses: actions/checkout@v3
    - name: Railway auto-deploy
      run: echo "Railway deploys automatically on push to main via GitHub integration"

  mobile:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
        cache: 'npm'
        cache-dependency-path: mobile/package-lock.json
    - name: Install dependencies
      run: cd mobile && npm ci --legacy-peer-deps
    - name: Type check
      run: cd mobile && npx tsc --noEmit
    - name: Build preview APK
      if: ${{ secrets.EXPO_TOKEN != '' }}
      run: cd mobile && npx eas build --platform android --profile preview --non-interactive
      env:
        EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}