# Kaero Web — Environment Variables
# Copy to .env.local for local dev, set in Vercel dashboard for production

# Your Railway backend URL (no trailing slash)
VITE_API_URL=https://YOUR-RAILWAY-DOMAIN.up.railway.app/api/v1
